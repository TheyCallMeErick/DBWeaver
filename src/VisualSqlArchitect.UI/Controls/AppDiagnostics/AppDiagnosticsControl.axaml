<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:VisualSqlArchitect.UI.ViewModels"
             xmlns:loc="using:VisualSqlArchitect.UI.Services.Localization"
             xmlns:mi="using:Material.Icons.Avalonia"
             x:Class="VisualSqlArchitect.UI.Controls.AppDiagnosticsControl"
             x:DataType="vm:AppDiagnosticsViewModel">

  <UserControl.Styles>
    <Style Selector="Border.diag-row">
      <Setter Property="Background"    Value="Transparent"/>
      <Setter Property="CornerRadius"  Value="8"/>
      <Setter Property="Padding"       Value="12,9"/>
      <Setter Property="Margin"        Value="4,2"/>
      <Setter Property="Transitions">
        <Transitions>
          <BrushTransition Property="Background" Duration="0:0:0.08"/>
        </Transitions>
      </Setter>
    </Style>
    <Style Selector="Border.diag-row:pointerover">
      <Setter Property="Background" Value="#1A1E2C"/>
    </Style>
  </UserControl.Styles>

  <!-- ══ Floating diagnostics panel ════════════════════════════════════════════ -->
  <Border Width="520"
          CornerRadius="12"
          Background="#13161F"
          BorderBrush="#252C3F"
          BorderThickness="1"
          BoxShadow="0 8 40 0 #80000000, 0 2 8 0 #60000000">

    <StackPanel>

      <!-- ── Header ───────────────────────────────────────────────────────── -->
      <Border Background="#1A1E2C"
              CornerRadius="12,12,0,0"
              Padding="16,12">
        <Grid ColumnDefinitions="Auto,*,Auto,Auto">

          <!-- Pulse icon -->
          <mi:MaterialIcon Grid.Column="0"
                           Kind="HeartPulse"
                           Width="18" Height="18"
                           Foreground="{Binding OverallColor}"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

          <!-- Title + overall status -->
          <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
            <TextBlock Text="{Binding [diagnostics.title], Source={x:Static loc:LocalizationService.Instance}}"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="#E8EAED"/>
            <TextBlock Text="{Binding OverallLabel}"
                       FontSize="10"
                       Foreground="{Binding OverallColor}"/>
          </StackPanel>

          <!-- Run button -->
          <Button Grid.Column="2"
                  Command="{Binding RunChecksCommand}"
                  Background="#1E2335"
                  BorderBrush="#252C3F"
                  BorderThickness="1"
                  CornerRadius="6"
                  Padding="10,5"
                  Margin="0,0,6,0"
                  Cursor="Hand"
                  ToolTip.Tip="{Binding [diagnostics.tooltip.rerun], Source={x:Static loc:LocalizationService.Instance}}"
                  AutomationProperties.Name="Run diagnostics">
            <StackPanel Orientation="Horizontal" Spacing="4">
              <mi:MaterialIcon Kind="Refresh" Width="13" Height="13" Foreground="#60A5FA"/>
              <TextBlock Text="{Binding [diagnostics.run], Source={x:Static loc:LocalizationService.Instance}}" FontSize="11" Foreground="#60A5FA"/>
            </StackPanel>
          </Button>

          <!-- Copy report button -->
          <Button Grid.Column="3"
                  Command="{Binding CopyReportCommand}"
                  Background="Transparent"
                  BorderThickness="0"
                  CornerRadius="6"
                  Padding="8,5"
                  Margin="0,0,6,0"
                  Cursor="Hand"
                  ToolTip.Tip="{Binding [diagnostics.tooltip.copy], Source={x:Static loc:LocalizationService.Instance}}"
                  AutomationProperties.Name="Copy report">
            <mi:MaterialIcon Kind="ContentCopy" Width="15" Height="15" Foreground="#4A5568"/>
          </Button>

          <!-- Close button -->
          <Button Grid.Column="3"
                  Command="{Binding CloseCommand}"
                  Background="Transparent"
                  BorderThickness="0"
                  CornerRadius="6"
                  Padding="8,5"
                  Cursor="Hand"
                  ToolTip.Tip="{Binding [diagnostics.tooltip.close], Source={x:Static loc:LocalizationService.Instance}}"
                  AutomationProperties.Name="Close diagnostics">
            <mi:MaterialIcon Kind="Close" Width="14" Height="14" Foreground="#4A5568"/>
          </Button>
        </Grid>
      </Border>

      <!-- Divider -->
      <Border Height="1" Background="#1E2335"/>

      <!-- Running indicator -->
      <Border Padding="16,8"
              IsVisible="{Binding IsRunning}"
              Background="#0F1A30">
        <TextBlock Text="{Binding [diagnostics.running], Source={x:Static loc:LocalizationService.Instance}}"
                   FontSize="11"
                   Foreground="#60A5FA"
                   HorizontalAlignment="Center"/>
      </Border>

      <!-- ── Entries list ──────────────────────────────────────────────────── -->
      <ScrollViewer MaxHeight="380" HorizontalScrollBarVisibility="Disabled">
        <ItemsControl ItemsSource="{Binding Entries}" Margin="0,4,0,4">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vm:AppDiagnosticEntry">

              <Border Classes="diag-row">
                <Grid ColumnDefinitions="28,*,Auto">

                  <!-- Status icon -->
                  <Border Grid.Column="0"
                          Width="22" Height="22"
                          CornerRadius="5"
                          Background="#1E2335"
                          VerticalAlignment="Center">
                    <mi:MaterialIcon Kind="{Binding StatusIconKind}"
                                     Width="14" Height="14"
                                     Foreground="{Binding StatusColor}"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                  </Border>

                  <!-- Name + details + recommendation -->
                  <StackPanel Grid.Column="1" Spacing="2"
                              VerticalAlignment="Center" Margin="10,0,8,0">
                    <TextBlock Text="{Binding Name}"
                               FontSize="12"
                               FontWeight="Medium"
                               Foreground="#E8EAED"/>
                    <TextBlock Text="{Binding Details}"
                               FontSize="10"
                               Foreground="#4A5568"
                               TextWrapping="Wrap"/>
                    <TextBlock FontSize="10"
                               Foreground="#FBBF24"
                               TextWrapping="Wrap"
                               IsVisible="{Binding Status,
                                 Converter={x:Static ObjectConverters.IsNotNull}}"
                               Text="{Binding Recommendation}">
                      <TextBlock.IsVisible>
                        <MultiBinding Converter="{x:Static BoolConverters.And}">
                          <Binding Path="Status"
                                   Converter="{x:Static ObjectConverters.IsNotNull}"/>
                          <Binding Path="Status"
                                   ConverterParameter="0"
                                   Converter="{x:Static ObjectConverters.IsNotNull}"/>
                        </MultiBinding>
                      </TextBlock.IsVisible>
                    </TextBlock>
                  </StackPanel>

                  <!-- Timestamp -->
                  <TextBlock Grid.Column="2"
                             Text="{Binding LastCheckLabel}"
                             FontSize="10"
                             Foreground="#252C3F"
                             VerticalAlignment="Center"
                             FontFamily="Consolas,Monospace"/>
                </Grid>
              </Border>

            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>

      <!-- ── Footer ───────────────────────────────────────────────────────── -->
      <Border Background="#0E1018"
              CornerRadius="0,0,12,12"
              Padding="12,8">
        <StackPanel Orientation="Horizontal" Spacing="16">
          <StackPanel Orientation="Horizontal" Spacing="4">
            <Border Background="#1E2335" CornerRadius="3" Padding="4,1">
              <mi:MaterialIcon Kind="CheckCircle" Width="12" Height="12" Foreground="#4ADE80"/>
            </Border>
            <TextBlock Text="{Binding [diagnostics.ok], Source={x:Static loc:LocalizationService.Instance}}" FontSize="10" Foreground="#4A5568" VerticalAlignment="Center"/>
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="4">
            <Border Background="#1E2335" CornerRadius="3" Padding="4,1">
              <mi:MaterialIcon Kind="Alert" Width="12" Height="12" Foreground="#FBBF24"/>
            </Border>
            <TextBlock Text="{Binding [diagnostics.warning], Source={x:Static loc:LocalizationService.Instance}}" FontSize="10" Foreground="#4A5568" VerticalAlignment="Center"/>
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="4">
            <Border Background="#1E2335" CornerRadius="3" Padding="4,1">
              <mi:MaterialIcon Kind="CloseCircle" Width="12" Height="12" Foreground="#EF4444"/>
            </Border>
            <TextBlock Text="{Binding [diagnostics.error], Source={x:Static loc:LocalizationService.Instance}}" FontSize="10" Foreground="#4A5568" VerticalAlignment="Center"/>
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="4">
            <Border Background="#1E2335" CornerRadius="3" Padding="4,1">
              <TextBlock Text="Esc" FontSize="10" Foreground="#4A5568"/>
            </Border>
            <TextBlock Text="{Binding [common.close], Source={x:Static loc:LocalizationService.Instance}}" FontSize="10" Foreground="#4A5568" VerticalAlignment="Center"/>
          </StackPanel>
        </StackPanel>
      </Border>

    </StackPanel>
  </Border>
</UserControl>
